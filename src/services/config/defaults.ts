/**
 * Default path to the inlang project directory, relative to the working
 * directory of the integration (CLI: process cwd; bundler plugins: the
 * tool's project root).
 */
export const DEFAULT_PROJECT_PATH = "./project.inlang";

/**
 * Default output directory for compiled messages, relative to the working
 * directory of the integration.
 */
export const DEFAULT_OUTDIR = "./src/paraglide";
