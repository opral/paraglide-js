---
"@inlang/paraglide-js": patch
---

Fix plural exact-match code generation so numeric cases compile against the raw input, outrank category matches and catchalls, and honor plural offsets during category selection.
